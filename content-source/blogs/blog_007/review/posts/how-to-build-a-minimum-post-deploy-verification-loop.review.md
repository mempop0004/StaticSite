# Review

- Rewrote the post around a real minimum release verification loop instead of abstract “check after deploy” advice.
- Added explicit pass/fail/review states and a user-pain-based check order.
- Added internal links to the unit page, the baseline ops post, and the RSS/sitemap release gate post.
- Updated image prompts toward document/data visuals instead of decorative dashboard graphics.

# Decision

- Ready for image-backed publish flow and deploy sync.
