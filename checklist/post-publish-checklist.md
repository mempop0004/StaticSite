# Post Publish Checklist

- 제목이 검색 의도를 반영하는가
- 설명이 RSS와 메타 설명으로 써도 되는가
- frontmatter 필드가 빠지지 않았는가
- 링크, 코드 블록, 문단 구조가 깨지지 않았는가
- Codespace에서 `npm run build`가 통과하는가
- 홈, 글 목록, 글 상세에서 어색한 표시가 없는가
- Cloudflare Pages 반영 후 실서비스 화면이 정상인가
- push 전에 queue 상태가 현재 작업과 맞는가
